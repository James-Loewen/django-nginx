name: build images

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Image Meta
        id: image-meta
        run: |
          SHORT_REF=${GITHUB_SHA::8}

          DJANGO_IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/django-image
          NGINX_IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/nginx-image

          DJANGO_TAGS="${DJANGO_IMAGE}:${SHORT_REF},${DJANGO_IMAGE}:latest"
          NGINX_TAGS="${NGINX_IMAGE}:${SHORT_REF},${NGINX_IMAGE}:latest"

          # Set output parameters
          echo "DJANGO_TAGS=${DJANGO_TAGS}" >> "$GITHUB_OUTPUT"
          echo "NGINX_TAGS=${NGINX_TAGS}" >> "$GITHUB_OUTPUT"
      
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Set up QEMU
        uses: docker/setup-qemu-action@master
        with:
          platforms: all
      
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Build and Push Django
        uses: docker/build-push-action@v4
        with:
          builder: ${{ steps.buildx.outputs.name }}
          context: ./backend/.
          file: ./backend/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.image-meta.outputs.DJANGO_TAGS }}

      - name: Build and Push Nginx
        uses: docker/build-push-action@v4
        with:
          builder: ${{ steps.buildx.outputs.name }}
          context: ./frontend/.
          file: ./frontend/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.image-meta.outputs.NGINX_TAGS }}